package com.example.movieapp.features.auth.credentials

import androidx.compose.ui.util.trace
import javax.inject.Inject

class Authenticator @Inject constructor() {
    fun isLogin(): Boolean {
        return true
    }
}